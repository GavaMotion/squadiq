node_modules
dist
.env
.env.local
supabase/.env

.vercel

.claude/settings.local.json

# Secrets — never commit tokens/keys
*.pem
github_pat_*
**/github_pat_*
*token*.txt

# Android (Bubblewrap TWA) build outputs & signing material
android/build/
android/.gradle/
android/app/build/
android/*.keystore
android/*.jks
android/local.properties
